apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ottoke.fullname" . }}-config
  labels:
    {{- include "ottoke.labels" . | nindent 4 }}
data:
  ALLOWED_ORIGINS: {{ .Values.backend.allowedOrigins | quote }}
